using Flunt.Notifications;

namespace Dominio.Interface.Comando.ComandoBase
{
	public interface IComando : INotifiable
	{
		//Task<int> Cadastrar(string nome,string email,string senha);
		//Task<int> Editar(Guid id,string nome,string email,string senha);
		//Task<int> Deletar(Guid guid);
	}
}
